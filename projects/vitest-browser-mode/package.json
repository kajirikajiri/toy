{
  "scripts": {
    "test": "vitest --config vitest.config.ts",
    "browser": "vitest --config vitest-browser.config.ts"
  },
  "devDependencies": {
    "@vitest/browser": "^1.1.0",
    "vitest": "^1.1.0",
    "webdriverio": "^8.27.0"
  }
}
