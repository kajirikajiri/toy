version: '3.8'

services:
  web:
    build: .
    ports:
      - "4673:4673"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4673/health"]
      interval: 30s
      timeout: 10s
      retries: 3
